<%= render "layouts/header" %> 
<div class="Container">
    <div class="row justify-content-center">
        <div class="col-8 text-center">
            <h1 class="pt-3"><u> <%= @recette.titre %> </u></h1>
        <div>
        <hr />
    </div>
    <div class="row">
        <div class="col-12">
            <h4 class="text-center"> <%= @recette.etapes %> </h4>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-4">
            <h3 class="text-center"><u>Ingrédients</u></h3>
            <ul class="list-group list-group-flush">
                <% @recette.recette_ingredients.each do |recette_ingredients| %>
                    <li class="list-group-item"> <%= recette_ingredients.ingredient.nom %> <%=  recette_ingredients.quantite %> <%= recette_ingredients.unite %>   </li>
                <% end %>
            </ul>  
        </div>
        <hr />
        <br />
    </div>
</div>