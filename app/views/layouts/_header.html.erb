<nav class="navbar navbar-dark bg-dark navbar-expand-lg">
    <div class="container-fluid">
    
        <a class="navbar-brand" href="#">
            
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <% if user_signed_in?%>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="<%= root_path %>">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= mesrecettes_path %>">Mes Recettes</a>
                </li>
                <% if current_user.is_admin?%>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="/admin/recettes">Recettes</a>
                    </li>
                <% end %>
                </ul>

                <% if user_signed_in? %>
                    <ul class="nav justify-content-end">
                        <li class="nav-item">
                            <a class="nav-link disabled" href="#">Bonjour <%= current_user.nom%> <%= current_user.prenom%></a>
                        </li>
                        
                    </ul>
                <% end %>
                
                <a class="d-flex btn btn-outline-danger" href="<%= destroy_user_session_path %>" method="delete" data-method="delete" >Log out</a>
            <% else %>
                <a class="d-flex btn btn-outline-success" href="<%= new_user_session_path %>" method="delete" >Log in</a>
            <% end %>
        </div>
    </div>
</nav>

<div class="container mt-3">
    <% if notice %>
        <div class="alert alert-primary d-flex align-items-center" role="alert">
        <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Info:"><use xlink:href="#info-fill"/></svg>
        <div>
            <%= notice %>
        </div>
        </div>
    <% end %>
    <% if alert %>
        <div class="alert alert-danger d-flex align-items-center" role="alert">
        <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Danger:"><use xlink:href="#exclamation-triangle-fill"/></svg>
        <div>
            <%= alert %>
        </div>
        </div>
    <% end %>
</div>