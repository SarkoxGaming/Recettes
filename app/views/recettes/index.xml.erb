<xml>
    <ingredients>
        <% @ingredients.select('nom').map(&:nom).to_a.each do |ingredient| %>
            <ingredient><%= ingredient %></ingredient>
        <% end %>        
    </ingredients>
</xml>